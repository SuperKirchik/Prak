# Task2 - Решение задач по программированию

## 📁 Что здесь находится?

В этой папке решения трех задач:

### 🔢 Task 2.2.1 - Вычисление многочлена
- **Что делает:** Считает значение многочлена в точке X
- **Как работает:** Использует быстрый метод Горнера
- **Ввод:** `X a_n a_{n-1} ... a_0` (числа через пробел)
- **Вывод:** значение многочлена в точке X

### 📈 Task 2.2.2 - Многочлен + производная
- **Что делает:** Считает значение многочлена И его производной в точке X
- **Ввод:** `X a_n a_{n-1} ... a_0`
- **Вывод:** 
  - значение многочлена
  - значение производной

### ∫ Task 2.2.3 - Многочлен + производная + интеграл
- **Что делает:** Считает многочлен, производную и интеграл от 0 до X
- **Ввод:** `n X a_n a_{n-1} ... a_0` (n - степень многочлена)
- **Вывод:**
  - значение многочлена
  - значение производной  
  - значение интеграла

### 🐰 Task 2.3 - Числа Фибоначчи
- **Что делает:** Считает i-е число Фибоначчи двумя способами
- **Как работает:** 
  - Быстрый итеративный метод (цикл)
  - Медленный рекурсивный метод (вызов самой себя)
- **Ввод:** числа i (по одному в строке)
- **Вывод:** для каждого i:
  - число Фибоначчи (итеративно)
  - число Фибоначчи (рекурсивно)

## 🚀 Как запускать?

1. **Компилируем программу:**
   ```bash
   gcc program.c -o program

# Task 2 Solutions

## Task 2.4 - String to Double Conversion

### Файл: `task2.4.c`

**Задача:** Реализовать функцию `str2double()` для преобразования строки в число типа `double` без использования стандартных функций.

**Особенности:**
- Обрабатывает числа в формате языка C (целые, дробные, экспоненциальная запись)
- Поддерживает знаки `+`, `-`
- Обрабатывает десятичную точку и экспоненту (`e`, `E`)
- Проверяет корректность входных данных


## Task 2.5 - List of Words with Duplicate Removal

### Файл: `task2.5.c`

**Задача:** Построить список слов из входной строки и удалить все вхождения последнего слова (кроме самого последнего).

**Алгоритм:**
1. Чтение слов из stdin через `scanf("%s")`
2. Построение односвязного списка
3. Поиск последнего слова
4. Удаление всех его вхождений (кроме последнего)
5. Сортировка списка по длине слов
6. Вывод результата

**Структура данных:**
```c
struct WordNode {
    char* word;
    struct WordNode* next;
};