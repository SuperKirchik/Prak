// Это мой шелл (shell) программа
// Делал сам, разбирался с кодом

// ЧТО УМЕЕТ:
// 1. Запускать команды: ls, grep, wc и другие
// 2. Конвейеры: ls | grep txt | wc -l
// 3. Перенаправления: 
//    - ls > file.txt  (записать в файл)
//    - sort < data.txt (читать из файла)
//    - echo hi >> log.txt (добавить в конец)
// 4. Фоновый режим: sleep 10 &
// 5. && и || : ls /tmp && echo "ok" || echo "fail"
// 6. Встроенные команды: cd, pwd, exit

// КАК РАБОТАЕТ:
// 1. Читает строку команды
// 2. Разбивает на слова (лексер)
// 3. Проверяет синтаксис (нельзя | ls, нужно ls | grep)
// 4. Заменяет $HOME, $USER на значения
// 5. Строит дерево команд
// 6. Выполняет команды через fork/exec
// 7. Для конвейеров использует pipe

// КАК СОБРАТЬ:
// make
// ./myshell

// КАК ПОЛЬЗОВАТЬСЯ:
// => ls -la
// => cd /tmp
// => pwd
// => ls | grep .c
// => echo "test" > file.txt
// => sleep 5 &


// ЧТО НЕ СДЕЛАЛ (по заданию не требовалось):
// - Скобки ()
// - Командные файлы
// - If/for конструкции